import {
  GREY_TEXT,
  LIGHT_GREY,
  PRIMARY_LIGHT,
  SECONDARY,
} from "@/styles/colors";
import { SxProps, Theme } from "@mui/system";

export const styles = {
  container: {
    width: "100%",
    height: "100%",
    display: "flex",
    flexDirection: "column",
  },
} as const;
